server:
  port: 8083

spring:
  application:
    name: ride-service

  datasource:
    url: jdbc:mysql://localhost:3306/carpool_ride
    username: root
    password: "@Harsh007"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092
    topic:
      notifications: notifications

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

jwt:
  secret: "qwertyuiopasdfghjklzxcvbnmqwertyuiop"